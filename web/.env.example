# Base URL of the API. Optional locally -- defaults to http://localhost:8080.
# In production this is set in the host's dashboard to the deployed API URL.
#
# NOTE: VITE_ variables are inlined into the built JavaScript and are public.
# Never put a secret behind this prefix.
VITE_API_BASE_URL=http://localhost:8080
